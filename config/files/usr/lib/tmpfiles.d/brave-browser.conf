# Create a symlink at /opt/brave.com (which resolves to /var/opt/brave.com)
# pointing to the actual install location in the immutable rootfs.
# This runs at boot via systemd-tmpfiles-setup.service.
L /var/opt/brave.com - - - - /usr/lib/opt/brave.com
